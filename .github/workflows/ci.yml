name: CI

on:
  push:
    branches: [main]
  pull_request:
  # Manual trigger, so a run can be started without inventing a commit —
  # a run that gets stranded (a GitHub incident mid-queue leaves one that
  # can be neither cancelled nor rerun) otherwise needs a push to replace.
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      # Typecheck, lint and test as separate steps rather than `npm run
      # check`, so a failing run names which one broke without anyone
      # having to open the log.
      - name: Typecheck
        run: npx tsc -b
      - name: Lint
        run: npm run lint
      - name: Test
        run: npm test
